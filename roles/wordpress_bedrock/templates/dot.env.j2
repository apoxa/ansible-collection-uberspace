DB_NAME='{{ uberspace_wordpress__database_name }}'
DB_USER='{{ ansible_facts.user_id }}'
DB_PASSWORD='{{ uberspace_mysql_password }}'

# Optionally, you can use a data source name (DSN)
# When using a DSN, you can remove the DB_NAME, DB_USER, DB_PASSWORD, and DB_HOST variables
# DATABASE_URL='mysql://database_user:database_password@database_host:database_port/database_name'

# Optional database variables
# DB_HOST='localhost'
# DB_PREFIX='wp_'

WP_ENV='production'
WP_HOME='https://{{ uberspace_wordpress__domain }}'
WP_SITEURL="${WP_HOME}/wp"

# Specify optional debug.log path
# WP_DEBUG_LOG='/path/to/debug.log'

# Generate your keys here: https://roots.io/salts.html
AUTH_KEY='{{ uberspace_wordpress__auth_key }}'
SECURE_AUTH_KEY='{{ uberspace_wordpress__secure_auth_key }}'
LOGGED_IN_KEY='{{ uberspace_wordpress__logged_in_key }}'
NONCE_KEY='{{ uberspace_wordpress__nonce_key }}'
AUTH_SALT='{{ uberspace_wordpress__auth_salt }}'
SECURE_AUTH_SALT='{{ uberspace_wordpress__secure_auth_salt }}'
LOGGED_IN_SALT='{{ uberspace_wordpress__logged_in_salt }}'
NONCE_SALT='{{ uberspace_wordpress__nonce_salt }}'
